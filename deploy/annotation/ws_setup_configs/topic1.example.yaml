# Example setup config for one annotation topic.
# Copy this file, rename "topic1" to your topic slug, and adjust workspace
# names and overlap thresholds as needed.
# Pass with --config to `pragmata annotation setup` and `pragmata annotation import`.
#
# See deploy/annotation/README.md for all available fields and their defaults.

workspace_dataset_map:
  topic1-retrieval:
    retrieval: {}
  topic1-grounding:
    grounding: {}
  topic1-generation:
    generation: {}
